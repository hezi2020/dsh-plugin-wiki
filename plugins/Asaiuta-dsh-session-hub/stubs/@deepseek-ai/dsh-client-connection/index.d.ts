/** Typecheck stub: @deepseek-ai/dsh-client-connection/client (re-exports only; the plugin
 *  consumes it only through peer types today). */

export type SessionId = string & { readonly __sessionId: unique symbol }