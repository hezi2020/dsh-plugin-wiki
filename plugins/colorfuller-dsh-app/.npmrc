# Keep runtime dependency installation quiet and deterministic.
# A custom registry mirror can be added here if the default registry is slow.
audit=false
fund=false
update-notifier=false
progress=false
